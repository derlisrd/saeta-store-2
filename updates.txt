ALTER TABLE `facturas` ADD `factura_vuelto` FLOAT NULL DEFAULT '0' AFTER `recibido_factura`; 
ALTER TABLE `empresa_facturas` ADD `activo` TINYINT(1) NOT NULL DEFAULT '1' COMMENT '0 no 1 si' AFTER `autoimpresor`;
CREATE TABLE `facturas_cajas` (
  `id_facturas_caja` int NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `caja_id_factura` int NOT NULL COMMENT 'caja',
  `factura_empresa_id` int NOT NULL COMMENT 'empresa_facturas',
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP
);